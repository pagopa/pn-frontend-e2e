selenium:
  browser: chrome
  headless: false
  arguments:
    - --window-size=1920,1080

url:
  selfcare:
    notifiche:
      base: https://selfcare.test.notifichedigitali.it
      dashboard: ${url.selfcare.notifiche.base}/dashboard

token:
  mittente: ${pn.bearer-token.tokentestMittente}
